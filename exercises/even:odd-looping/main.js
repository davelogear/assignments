//ussing the % to find the remander after I deive by two

for(var numCount = 0; numCount < 101; numCount++){
    if(numCount % 2 === 0){
        console.log(numCount + "is even")
    } else {
        console.log("is odd")
    }
}